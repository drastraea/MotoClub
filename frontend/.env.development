# Loaded automatically by Next.js for `npm run dev` (development).
NEXT_PUBLIC_API_URL=http://localhost:8080

# Set to your Google OAuth Client ID (authorized origin http://localhost:3000)
# to enable Google Sign-In in development. Must match the backend's
# google_client_id for the `local` environment in ../backend/config.yaml.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=271720379050-j4vh9raaoeo58gqlf5gli1moh8g38srt.apps.googleusercontent.com
