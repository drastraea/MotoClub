# Loaded automatically by Next.js for `npm run build` (production).
# These NEXT_PUBLIC_* values are baked into the client bundle at build time.
# Override at Docker build with --build-arg if needed.
NEXT_PUBLIC_API_URL=https://api.motoclub.example

# Production Google OAuth Client ID (authorized origin = your prod domain).
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
