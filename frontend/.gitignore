# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
*.tsbuildinfo
next-env.d.ts

# env files — ignore local/secret ones, but track the committed per-env configs.
# NEXT_PUBLIC_* values are public (shipped to the browser), so these are safe.
.env*
!.env.development
!.env.production
!.env.example

# vercel
.vercel

# agent docs (internal, not part of shipped project)
AGENTS.md
CLAUDE.md
